= form_for @update, html: { class: 'form-stacked' } do |f|
  .clearfix
    = f.label :content, "Something to say?"
    .input
      = f.text_area :content, placeholder: "Put it here!", class: "span7", rows: "3"
  .actions
    = f.submit "Share", class: "btn primary"