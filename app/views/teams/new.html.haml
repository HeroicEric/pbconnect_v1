= form_for @team do |f|
  .clearfix
    = f.label :name, "Name"
    .input
      = f.text_field :name
  .actions
    = f.submit "Save", class: "btn primary"
    = link_to "Cancel", :back, class: "btn secondary"