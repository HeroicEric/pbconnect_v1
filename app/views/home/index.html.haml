%br
%br
- if current_user
  = render 'shared/update_form'

%h1 User List
- if @users.empty?
  %p Sorry, there are no users yet.
- else
  %ul
    - @users.each do |user|
      %li User: #{link_to user.name, user_path(user)}